

Count() GROUP BY

SELECT Region, Division, COUNT( state ) 
FROM  `unEmpRate` 
GROUP BY Region, Division

--------------


SUM() GROUP BY

SELECT Region, SUM(Population) 
FROM  `statePop` 
GROUP BY Region

------

Sum(), GROUP BY, HAVING


SELECT Region, SUM( Population ) 
FROM  `statePop` 
GROUP BY Region
HAVING SUM( Population ) >70000000



------

AVG() GROUP BY

SELECT Region, Division, AVG( Rate ) 
FROM  `unEmpRate` 
GROUP BY Region, Division

